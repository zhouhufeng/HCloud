# Never commit credentials
docs/Secretes/
sudo-secret.txt
*sudo*secret*
pull-secret.txt
*pull-secret*
*.pem
*.key

# migrated PVC data (never commit)
cluster-data/

# HCloud platform contract + platform state (per-node / per-deployment, never shared)
hcloud/hcloud.env
platform/**/.terraform/
platform/**/*.tfstate*
platform/**/*.tfvars

# Jetstream2 backup target (multi-TB; never commit)
JetstreamData/
